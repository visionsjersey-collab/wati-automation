services:
  - type: worker
    name: wati-auto-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python main.py
    plan: free
