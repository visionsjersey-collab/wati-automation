services:
  - type: web
    name: wati-auto-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python main.py
    plan: free
    envVars:
      - key: PORT
        value: 10000
